# tablex

table 工具库，收纳排序遍历、复制、合并等通用表操作。

- 保持纯 Lua table 输入输出。
- 适合配置表、状态表和映射表处理。
- 避免业务模块重复实现小工具。

## 设计特性

### 表操作收口

项目里反复出现的 table 操作集中到这里。新增工具时优先保持函数小而明确，避免把领域规则写进基础库。

### 稳定遍历

需要确定顺序的遍历由库统一提供，调用方不用在每个模块里重复收集 key 和排序。

## 目录

```text
init.lua  # table 通用工具
```
