# point

二维点工具库，用统一结构表达世界坐标或平面坐标。

- 点对象使用 `x`、`y` 字段。
- 适合与几何、运动、地形等模块共用。
- 保持纯 Lua 数据，不绑定引擎坐标对象。

## 设计特性

### 轻量坐标

点只是普通 table，便于序列化、比较和传入其他库。需要高度时由调用方额外携带 `z` 或独立字段。

### 跨模块共享

运动、几何、地形绘制都可以用同一种点结构，避免每个模块定义自己的坐标字段。

## 目录

```text
init.lua  # 点对象与基础坐标工具
```
