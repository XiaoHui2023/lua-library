# debugx

调试输出辅助库，提供可替换的序列化、打印和栈信息入口。

- 默认不绑定具体日志系统。
- 运行时可以注入更适合宿主的输出后端。
- 适合库代码输出结构化调试信息。

## 设计特性

### 后端注入

库层只保留通用调试入口。宿主如果有自己的日志、颜色或文件输出能力，可以在运行时注入后端。

### 结构输出

调试内容可以先被规范化为字符串，再交给输出后端。业务侧不需要在每处重复判断日志环境。

## 目录

```text
init.lua  # 调试输出入口与后端注入
test.lua  # 调试输出测试
```
